#rnd: random number determining the transformation type
def TransformImage(image, rnd):
    return image

#Transforms a batch of images from Dataset into a batch of augmented image pairs
def CreateImagePairBatch(imageBatch):
    pass